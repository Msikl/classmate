# ClassMate · 课表应用

一个学生课表（课程表）移动应用。当前仅你一人使用、单人开发，定位为**纯前端项目**，暂不引入后端。后端相关技术约定（Spring Boot / JDK 21 / 阿里规范 / `{code,msg,data}`）已搁置，等将来确实需要服务端再启用。

仓库当前只有 `README.md` 与 `.git/`，尚无源码，初始化阶段不要假设存在已实现功能。


## 技术栈（已确认）

- 前端：Vue 3 + TypeScript + Vite
- 移动端打包：Capacitor
- 代码规范：组合式 API + `<script setup>`；禁止使用 `any`
- 当前不引入：后端服务、数据库、状态管理持久化插件


## 项目全景（预期）

classmate  
├── src/  
│ ├── api/ # 请求封装（占位，暂不启用）  
│ ├── assets/ # 静态资源  
│ ├── components/ # 通用 UI 组件  
│ │ ├── common/ # 基础组件（按钮、弹窗、卡片）  
│ │ └── course/ # 课表业务组件（课程卡片、周视图、时间线）  
│ ├── composables/ # 组合式函数（useCourses, useWeek,useNotification）  
│ ├── layouts/ # 布局组件  
│ ├── router/ # 路由配置  
│ ├── stores/ # Pinia 状态管理
│ ├── types/ # TypeScript 类型定义  
│ ├── utils/ # 工具函数（日期、存储、通知）  
│ ├── views/ # 页面视图  
│ │ ├── HomeView.vue # 课表主页  
│ │ └── SettingsView.vue # 设置页  
│ ├── App.vue  
│ └── main.ts  
├── public/  
├── dist/ # 构建产物（git ignore）  
├── capacitor.config.ts  
├── index.html  
├── package.json  
├── tsconfig.json  
├── vite.config.ts  
└── README.md  



## 开发阶段目标

| 阶段 | 目标 | 说明 |
| :--- | :--- | :--- |
| **P0** | 课表展示 | 周视图网格 + 课程卡片 + 当前周次高亮 |
| **P1** | 课程管理 | 手动增删改课程 + 本地存储持久化 |
| **P2** | 开学日期设置 | 可配置开学日期，自动计算当前周次 |
| **P3** | 课前提醒 | 本地通知（系统通知栏），提前 N 分钟提醒 |
| **P4** | 教务同步 | 内嵌 WebView 登录教务系统，抓取课表（方案待定） |


## 数据模型（草稿）

```typescript
// 课程
interface Course {
  id: string
  name: string
  teacher?: string
  classroom?: string
  dayOfWeek: 1 | 2 | 3 | 4 | 5 | 6 | 7
  startTime: string   // "08:00"
  endTime: string     // "09:40"
  weeks: number[]     // [1,2,3,...,16]
  type?: 'single' | 'double' | 'all'
  note?: string
  color?: string
}

// 设置
interface Settings {
  startDate: string          // "2026-09-01"
  totalWeeks: number        // 默认 16
  notificationEnabled: boolean
  notificationMinutes: number // 默认 10
}

// 课程表数据结构（顶层）
interface ClassTableState {
  courses: Course[]
  settings: Settings
  syncState: {
    lastSyncTime?: string
    syncedCourseIds: string[]
  }
}  
```

## UI/UX 设计方向
课表主视图采用横向滚动周视图（周一至周日固定表头，每日按时间纵轴排列课程卡片），具体交互细节留待实现时由实际代码确定，目前不做过度设计。

## Build & test（占位，待源码落地后替换为精确调用）
- 依赖与开发：npm install → npm run dev（Vite dev server）

- 构建：npm run build（Vite 产物输出到 dist/）

- 移动端打包：npx cap add android/ios → npx cap sync → npx cap open

- 具体脚本一律以实际 package.json 的 scripts 为准，勿凭空编造命令

## Conventions（约定）
- 强制 `<script setup>` 组合式 API，页面逻辑按组合式函数（useXxx()）拆分

- 禁止任何 any（含隐式 any）——依赖 tsconfig 严格模式（noImplicitAny / strict）兜底，而非靠自觉

- 课表领域术语（课程、周次、节次、学期）命名保持一致，避免中英混拼

- 组件文件命名：PascalCase.vue（如 CourseCard.vue、WeekView.vue）

- 工具函数文件命名：camelCase.ts（如 dateUtils.ts、storageUtils.ts）

## Pitfalls
- 仓库为空：任何"该项目已有 X"的假设都以实际文件为准，先 find/ls 确认再动手

- 不要手改 Capacitor 生成的 android/、ios/ 或 www/ 构建产物，改源码后重新 cap sync

- 若发现 any 能编译通过，说明 tsconfig 未开启严格模式，需修正配置而非放任

- 遇到以下情形，请先沟通再执行，不要直接写代码：

  - 对仓库现有文件/目录进行 删除、重命名、移动

  - 新增 npm 包（需说明用途和必要性）

  - 修改 tsconfig.json、vite.config.ts、capacitor.config.ts 等配置文件

  - 引入 新的架构模式（如新增状态管理方案、新增网络层、新增持久化策略）

## AI 协作须知
- 每个功能开发按以下节奏推进：

1. 先说明实现思路
2. 确认方向后再生成代码
3. 代码落地后视反馈决定是否继续下一项或调整
- 若指令被判定为与现有架构冲突，应指出冲突并提供更合理的选择或提问澄清后再实施

## 后续扩展方向（暂不开发，仅备忘）
- 教务系统课表同步：通过内嵌 WebView 实现登录和课表抓取（方案细节待定）

- 课表数据导入/导出（Excel、ICS 格式）与备份恢复

- 多学期/多课表切换

- 桌面小部件（Widget）

## 变更记录
| 日期 | 版本 |	变更说明 |
| :---: | :---: | :---: |
|2026-08-23|v0.1|初始版本，建立项目骨架约定|