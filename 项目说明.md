# ClassMate · 课表应用

一个学生课表（课程表）**移动原生 APP**。**仅一人使用、单人开发**，定位为**纯前端项目**，暂不引入后端。后端相关技术约定（Spring Boot / JDK 21 / 阿里规范 / `{code,msg,data}`）已搁置，等将来确实需要服务端再启用。

## 产品定位（已确认）

- **形态**：移动端原生 APP，经 Capacitor 打包为 Android / iOS 应用；**不是网页，也不是小程序**。
- **使用**：仅本机单人使用。
- **包体结构**：以仓库内 `app/` 子目录为工程根，所有源码与配置约束在 `app/` 之下。仓库根目录只保留 `README.md` 与本文档。

## 技术栈（已确认）

- 前端：Vue 3 + TypeScript + Vite（`app/` 内脚手架已初始化并提交）
- 移动端打包：Capacitor（壳后置，P0–P3 阶段优先把 Web 侧做扎实，后期再加原生壳）
- 代码规范：组合式 API + `<script setup lang="ts">`；禁止使用 `any`
- 当前不引入：后端服务、数据库、状态管理持久化插件

## 项目全景（实际 + 预期）

> 结构**约束在 `app/` 子目录下**。下方为规划树，已存在项标注（已有），其余为预期新增，勿假设已实现。

```
classmate/
├── app/                          # 工程根（已是 Vue3+TS+Vite 脚手架，已提交）
│   ├── src/
│   │   ├── api/                  # 请求封装（占位，暂不启用）
│   │   ├── assets/               # 静态资源
│   │   ├── components/
│   │   │   ├── common/           # 基础组件（空，暂未建）
│   │   │   └── course/           # 已落地：CourseCard.vue / WeekView.vue / CourseDetailModal.vue
│   │   ├── composables/          # 已落地：useCourses.ts / useWeek.ts / useSchedule.ts
│   │   ├── layouts/              # 布局组件（空，暂未建）
│   │   ├── router/               # 路由配置（已有，/ → HomeView）
│   │   ├── stores/               # Pinia 状态管理（已有 counter.ts 占位，未用）
│   │   ├── types/                # 已落地：course.ts / schedule.ts
│   │   ├── utils/                # 工具函数（空，暂未建）
│   │   ├── views/                # 已落地：HomeView.vue
│   │   ├── App.vue               # 已实现（RouterView + 全局基础样式）
│   │   └── main.ts               # （已挂 Pinia + Router）
│   ├── public/
│   ├── index.html
│   ├── package.json
│   ├── tsconfig*.json
│   ├── vite.config.ts
│   └── README.md
├── README.md
└── 项目说明.md                   # 本文档，唯一事实依据
```

## 开发阶段目标

| 阶段 | 目标 | 说明 |
| :--- | :--- | :--- |
| **P0** | 课表展示 | 7 天固定周视图 + 课程卡片 + 当前周次高亮 |
| **P1** | 课程管理 | 手动增删改课程 + 本地存储持久化 |
| **P2** | 开学日期设置 | 可配置开学日期，自动计算当前周次 |
| **P3** | 课前提醒 | 本地通知（系统通知栏），提前 N 分钟提醒 |
| **P4** | 教务同步 | 内嵌 WebView 登录教务系统，抓取课表（方案待定） |

### P0 需求基线（已与用户确认）

- **周视图**：固定显示周一至周日 7 天，不横向滑动（与市面上课表 App 一致）。
- **课程卡片**：只读展示课程名 + 时间 + 教室三项。
- **当前周次**：P0 先写死占位常量（`currentWeek=1`、`totalWeeks=16`），P2 再接入日期计算。
- **课程规律**：一律按全学期处理（`type`/`weeks` 暂不参与过滤）。
- **课程颜色**：内置色卡自动循环分配，手动调色留到 P1。
- **数据来源**：内置种子数据（内存），不持久化（持久化归 P1）。

## 数据模型（落地为准）

> 课程时间采用**节次驱动**：课程记录「第几节到第几节」（`startPeriod/endPeriod`），具体绝对时间（时/分）由**节次表**推导；节次表 P0 内置默认常量，P2 起由「设置」驱动（每节时长 + 第 1 节起始时间 + 节间休息）重新生成，渲染层无需改动。

```typescript
// 课程 —— P0 实际使用 id/name/classroom/dayOfWeek/startPeriod/endPeriod/color
interface Course {
  id: string
  name: string
  teacher?: string          // 预留（种子数据已填充）
  classroom?: string
  dayOfWeek: 1 | 2 | 3 | 4 | 5 | 6 | 7
  startPeriod: number       // 起始节次（第几节，1-based）如 1
  endPeriod: number         // 结束节次（含）如 2
  weeks?: number[]          // 预留，全学期暂不参与过滤
  type?: 'single' | 'double' | 'all'  // 预留，全学期默认 'all'
  note?: string             // 预留
  color: string             // 自动分配，内置色卡
}

// 节次（schedule）
interface Period {
  index: number             // 节次序号 1-based
  startTime: string         // "08:00"
  endTime: string           // "08:45"
}

// 默认节次表（DEFAULT_PERIODS，@/types/schedule.ts）
// 第1~8节 08:00~17:40 + 晚1~2节 19:00~20:40，两小节一大节，每节45分，节间10分、大课间20分

// 设置
interface Settings {
  startDate: string            // "2026-09-01"
  totalWeeks: number           // 默认 16
  notificationEnabled: boolean
  notificationMinutes: number  // 默认 30（已确认，原草稿为 10）
}

// 课程表数据结构（顶层）
interface ClassTableState {
  courses: Course[]
  settings: Settings
  syncState: {                // 仅 P4（教务同步）启用，P0–P3 保持空置
    lastSyncTime?: string
    syncedCourseIds: string[]
  }
}
```

## UI/UX 设计方向（落地为准）

主视图为**课表区域独立垂直滚动 + sticky 表头**的周视图：
- 左列节次刻度轴（节次号 + 起止时间上下列）；右侧 7 天列（周一~周日，顶部表头含「周几 + 日期」）。
- 表头行 `position: sticky; top:0` 吸顶，课表区域滚动时表头/刻度不动。
- 网格：星期列之间为纵向竖线；7 天课程区为浅灰网格背景上的白色小卡片，卡片与网格留小间距。
- 课程卡片显示 课程名 / 起始·结束时间(上下两行) / 教室；点击弹出详情（节次、绝对时间、教室、老师）。
- 节次驱动：课程卡按「第几节到第几节」定位，与刻度列对齐。

## Build & test

依赖与开发脚本以 `app/package.json` 的 `scripts` 为准：

- 依赖安装：`npm install`（在 `app/` 目录内）
- 开发预览：`npm run dev`（Vite dev server，浏览器 preview）
- 构建 + 类型检查：`npm run build`（内部 `run-p type-check "build-only"`，含 vue-tsc 类型检查）
- 移动端打包（后期启用）：`npx cap add android/ios` → `npx cap sync` → `npx cap open`

> 接口验收：每阶段提交前须 `npm run build` 通过（含 vue-tsc 类型检查，且零 `any`）。

## Conventions（约定）

- 强制 `<script setup lang="ts">` 组合式 API，页面逻辑按组合式函数（`useXxx()`）拆分。
- 禁止任何 `any`（含隐式 any）——依赖 tsconfig 严格模式（`noImplicitAny` / `strict`）兜底，而非靠自觉。
- 课表领域术语（课程、周次、节次、学期）命名保持一致，避免中英混拼。
- 组件文件命名：`PascalCase.vue`（如 `CourseCard.vue`、`WeekView.vue`）。
- 工具函数文件命名：`camelCase.ts`（如 `dateUtils.ts`、`storageUtils.ts`）。

## Pitfalls

- **仓库已有 `app/` 与 `.git/`**：任何"该项目已有/没有 X"的假设都以实际文件为准，先 `find`/`ls` 确认再动手。
- 不要手改 Capacitor 生成的 `android/`、`ios/` 或 `www/` 构建产物，改源码后重新 `cap sync`。
- 若发现 `any` 能编译通过，说明 tsconfig 未开启严格模式，需修正配置而非放任。
- 遇到以下情形，请先沟通再执行，不要直接写代码：
  - 对仓库现有文件/目录进行 删除、重命名、移动（如清理 `stores/counter.ts`）
  - 新增 npm 包（需说明用途和必要性）
  - 修改 `tsconfig*.json`、`vite.config.ts`、`capacitor.config.ts` 等配置文件
  - 引入新的架构模式（如新增状态管理方案、新增网络层、新增持久化策略）

## 技术决策记录

| 日期 | 决策 | 说明 |
| :---: | :--- | :--- |
| 2026-08-28 | 工程根定为 `app/` | 以已提交的脚手架为起点，结构约束在 `app/` 下 |
| 2026-08-28 | 产品定位：移动原生 APP | Capacitor 打壳，非网页/小程序 |
| 2026-08-28 | 依赖版本保持现状 | pinia 4.x / vue-router 5.x / vue 3.5+ / vite 8 均为各自线最新正式版，无需调整 |
| 2026-08-28 | 周视图固定显示 7 天 | 不横向滑动，与市面课表 App 一致 |
| 2026-08-28 | 默认提醒时间 = 30 分钟 | 原草稿为 10 分钟，已确认改 30 |
| 2026-08-28 | **课程时间改「节次驱动」** | startPeriod/endPeriod 取代 startTime/endTime；绝对时间由节次表推导，P2 由设置驱动，渲染层无改 |
| 2026-08-28 | 卡片点击弹出课程详情 | CourseDetailModal（Teleport 实现），显示节次/绝对时间/教室/老师 |
| 2026-08-28 | 课表区域独立垂直滚动 + sticky 表头 | 刻度与课程一体，表头吸顶（P0 落地时确定） |

## AI 协作须知

- 每个功能开发按以下节奏推进：
  1. 先说明实现思路
  2. 确认方向后再生成代码
  3. 代码落地后视反馈决定是否继续下一项或调整
- 若指令被判定为与现有架构冲突，应指出冲突并提供更合理的选择或提问澄清后再实施。

## 后续扩展方向（暂不开发，仅备忘）

- 教务系统课表同步：通过内嵌 WebView 实现登录和课表抓取（方案细节待定）
- 课表数据导入/导出（Excel、ICS 格式）与备份恢复
- 多学期/多课表切换
- 桌面小部件（Widget）

## 变更记录

| 日期 | 版本 | 变更说明 |
| :---: | :---: | :--- |
| 2026-08-23 | v0.1 | 初始版本，建立项目骨架约定 |
| 2026-08-28 | v0.2 | 规范化：确定产品定位（移动 APP）、工程根（`app/`）、P0 需求基线（7 天固定视图/全学期/当前周写死/提醒 30 分钟/色卡自动分配）；同步实际目录；新增技术决策记录 |
| 2026-08-28 | v0.3 | **P0 落地并提交**：7 天周视图（节次刻度轴/sticky 表头/课表区域滚动/纵向网格线/小卡片）+ 课程详情弹窗；数据模型改节次驱动；同步目录树与数据模型文档 |
